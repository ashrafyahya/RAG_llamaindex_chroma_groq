# you need to install PyTorch (required for the embeddings)
pip install torch

# you need to install the Groq SDK
pip install groq-sdk

# you need to install the Hugging Face Transformers SDK
pip install transformers

# you need to install the LangChain SDK
pip install langchain


